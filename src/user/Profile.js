import React from "react";
import Base from "../core/Base";

const Profile = () => {
  return (
    <Base title="Profile" description="Welcome to Profile">
      <h1>this is Profile</h1>
    </Base>
  );
};

export default Profile;
